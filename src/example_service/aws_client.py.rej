@@ -1,14 +0,0 @@
-"""S3 client helper (AC-1 fixture: contains a planted secret).
-
-The hardcoded AWS secret access key below is a synthetic high-entropy value,
-not a live credential. gitleaks detects it, so the deterministic secret gate
-marks it blocking and the ai-review check fails (AC-1, requirement 6.2).
-AWS's documentation example key (AKIA.../...EXAMPLE) is intentionally avoided:
-secret scanners allowlist those placeholders, so they never trip the gate.
-"""
-
-AWS_SECRET_ACCESS_KEY = "wJ7kPq2Zr8sXt4Lm9NbVc3Df6Gh1Jk0Yp5Qa2Ww"
-
-
-def secret():
-    return AWS_SECRET_ACCESS_KEY
